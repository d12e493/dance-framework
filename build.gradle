group 'idv.danceframework'
version '1.0-SNAPSHOT'

allprojects {

    group "idv.danceframework"
    version "1.0.0-SNAPSHOT"

    apply plugin: 'idea'

    repositories {
        mavenCentral()
        jcenter()
    }
}

subprojects {
    ext {
        SpringWebmvcVersion = '4.3.2.RELEASE'
    }
}